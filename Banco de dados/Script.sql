-- BD BodyPedia

-- criando as tabelas

create table usuario(
			 id int not null primary key auto_increment,
             email varchar(100),
             nome varchar(100,
             senha varchar(20)
);
